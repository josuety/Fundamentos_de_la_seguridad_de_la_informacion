# Reto 
# ### Magikarp Ground Mission
# Descripción 
Do you know how to move between directories and read files in the shell? Start the container, `ssh` to it, and then `ls` once connected to begin.Login via `ssh` as `ctf-player` with the password, `8c606eb1` on the host `wily-courier.picoctf.net` and port `56460`.
# Solución
picoCTF{xxsh_0ut_0f_//4t3r_0b24fc4f}
# Notas adicionales
me movi entre directorios y use el cat y el more para seguir indicaciones y en 3 archivos estaba la flag y pues la encontre jjejeje
# Referencias